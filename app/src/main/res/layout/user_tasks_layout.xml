<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:background="@drawable/back"
    android:layout_height="match_parent">

    <android.support.v7.widget.RecyclerView
        android:id="@+id/user_tasks_recycler_view"
        android:layout_width="match_parent"
        android:scrollbars="vertical"
        android:layout_height="match_parent"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true" />

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/user_add_task"
        android:layout_width="125dp"
        android:layout_height="80dp"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true"
        android:clickable="true"
        android:focusable="true"
        app:backgroundTint="@android:color/holo_orange_light"
        app:srcCompat="@drawable/add" />

    <Button
        android:id="@+id/define_type_btn"
        android:layout_width="120dp"
        android:layout_height="50dp"
        android:layout_alignParentStart="true"
        android:layout_alignParentBottom="true"
        android:background="@android:color/holo_orange_light"
        android:clickable="true"
        android:focusable="true"
        android:text="@string/define_type"
        android:textColor="@android:color/background_light" />

    <Button
        android:id="@+id/set_type_btn"
        android:layout_width="80dp"
        android:layout_height="50dp"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true"
        android:layout_marginEnd="174dp"
        android:layout_marginBottom="0dp"
        android:background="@android:color/holo_orange_light"
        android:padding="0dp"
        android:text="Set Type"
        android:textColor="@android:color/background_light" />

    <Button
        android:id="@+id/set_task_btn"
        android:layout_width="80dp"
        android:layout_height="50dp"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true"
        android:layout_marginEnd="70dp"
        android:background="@android:color/holo_orange_light"
        android:text="Set Task"
        android:textColor="@android:color/background_light" />

</RelativeLayout>
